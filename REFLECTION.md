# REFLECTION.md — StoryFlow 项目反思报告


---

## 1. 整体体验：AI 协作开发与独立开发的差异

这是我第一次真正意义上用 AI Agent 完成一个完整的软件工程项目。与过去独立开发相比，最直观的感受是：我几乎不需要自己动手写代码，只需要有一个清晰的想法，并且能够持续地向 AI 提出正确的问题。

以往独立开发一个有 20+ 任务、覆盖后端 API、数据库、前端模板、安全治理、Docker 分发的项目，通常需要数周时间。这次 Claude Code 帮我在不到两周内完成了全部 24 个任务，代码量超过 15,000 行，测试覆盖率接近 100%。工作效率的提升是量级的。

但这并不意味着我什么都不用做。我的工作从"写代码"变成了"发现问题、提出问题、验证结果"。每一次生成的代码出现 bug，我需要准确地描述问题现象；每一次功能不符合预期，我需要判断是 AI 理解偏差还是我的需求没表达清楚。这种思维方式的转变，是这次项目给我最大的认知冲击。

---

## 2. 技术决策：主动做出的与 AI 建议后采纳的

**我主动做出的决策：**

- 选择构建一个**中文互动小说生成平台**（StoryFlow），而不是更常见的 Todo App 或博客系统。这是我个人的创意方向。
- 坚持使用**真实的关系型数据库**（SQLite）而非内存存储，确保故事数据在服务重启后不丢失。
- 决定支持**分支叙事**（Branch），让用户可以从历史选择点创建新的故事线，而不只是线性阅读。
- 决定接入 **Poe API** 而非直接使用 Anthropic API，因为这对我来说更方便。

**AI 建议后我采纳的决策：**

- 采用 **Director + Writer 双 LLM 调用模式**：Director 先生成结构化的场景计划（ScenePlan），Writer 再据此生成正文。这个分层设计是 Claude Code 提出的，实践证明大大提升了生成质量的可控性。
- 采用 **Worktree 并行开发**方式，每个 PR 在独立 worktree 里进行，避免分支冲突。
- 使用 **SSE（Server-Sent Events）** 实现流式生成，让用户实时看到文字逐字出现，而不是等待整段生成完毕。
- 在 API 路由层做**会话隔离**（X-Session-ID 头），确保不同用户的故事互不可见。

---

## 3. 挑战与突破：意料之外的技术难题

项目中遇到了几个完全没有预料到的问题：

**（1）LLM 响应格式不稳定**

这是最耗时的问题。Director 提示词要求模型返回特定 JSON 格式，但实际部署时（使用 Poe API），模型经常返回：
- `character_direction` 类型而不是 `decision`/`action`/`dialogue`
- `structured_effects` 键而不是 `effects`
- 故事正文里包含 `\"` 和 `\n\n` 等 JSON 转义字符

每次都需要加调试日志、读错误信息、写标准化函数来兼容模型的"自由发挥"。这让我深刻理解了为什么生产 LLM 应用需要大量的输入输出处理层，提示词本身只是一小部分工作。

**（2）Python 3.9 vs 3.12 兼容性**

项目在 worktree 里用 3.12 开发，合并到 main 后发现有些语法在 3.9 下不兼容（`datetime.UTC`、`typing.Self` 等）。最终在 `pyproject.toml` 里锁定 Python 3.12，并加了 `.python-version` 文件解决。

**（3）Docker 部署到 Render 的 PORT 问题**

Render 会动态分配端口并通过 `$PORT` 环境变量传入，但最初的 Dockerfile 把端口写死为 8000，导致容器启动后健康检查一直失败。改成 `${PORT:-8000}` 的 shell 形式后解决。

**（4）SQLite 数据库触发器**

分支创建时需要满足一系列复杂的外键约束（`fork_choice_id` 必须属于 `fork_segment`，`head_segment_id` 必须在父分支路径上等）。花了不少时间理解这些触发器的逻辑，并在测试里一条一条验证。

---

## 4. Agent 协作模式：Codex 与 Claude Code 的分工

本项目同时使用了 Codex（负责 PR-04，T08-T10 生成流）和 Claude Code（负责其余所有任务）。

**Codex 的体验：** 完成 PR-04 的三个任务耗时明显更长，消耗的 token 也更多。对话轮次多，常常需要反复澄清需求，有时生成的代码虽然能跑通，但和整体代码风格的一致性较差，需要额外的人工整合。

**Claude Code 的体验：** 效率明显更高。能够理解上下文、主动发现问题、按照 PLAN.md 的节奏推进任务。对于复杂的数据库操作和状态机逻辑，生成质量比 Codex 高出一个档次。

**我的干预点主要有：**
- 当生成的功能和业务逻辑有偏差时（比如选择效果字段名不符合 SPEC）提出修正
- 当测试失败但 AI 误判原因时，手动比对错误信息
- 部署阶段（Docker + Render）需要大量调试，AI 给出了正确的方向但具体参数需要反复试验
- 模型响应格式适配（LLM 本身的不可预测性 AI 无法完全预判）

---

## 5. 规约驱动开发：SPEC.md 的作用与冷启动验证

SPEC.md 在这个项目里发挥了真正的"合同"作用。每次 AI 生成代码后，我会对照 SPEC 里的验收标准来判断是否完成。如果 SPEC 里没写清楚，AI 就会做出它"认为合理"的实现，结果往往和我的预期不一致。

冷启动验证的收获是：它暴露了 SPEC 里一些我以为写清楚了、但其实模糊的地方。比如"选择效果应用到记忆"这条，我在 SPEC 里没有明确说明效果字段的格式，冷启动 Agent 自己推断了一套，和主开发思路不一样，最终需要增加标准化函数来兼容。这个经验让我意识到，规约的粒度要细到"字段名"级别，不能只描述功能意图。

---

## 6. TDD 体验：先测试再实现对代码质量的影响

这是我第一次严格执行 TDD 的项目。最终有 284 个测试，覆盖了单元测试、集成测试和端到端测试三个层次。

TDD 的直接收益是：当我修改某个模块时，能立刻知道有没有破坏其他功能。项目后期加了很多功能（分支、速率限制、Markdown 导出、会话隔离），每次合并前跑一遍全套测试，心里踏实很多。

最有趣的体验是：写失败测试的过程迫使我在实现之前就想清楚"这个功能的边界在哪里"。比如写选择提交的测试时，我需要想清楚：版本号不匹配怎么处理、故事不在等待选择状态怎么处理、重复提交怎么处理——这些边界条件如果不写测试，很可能在实现时根本想不到。

---

## 7. 课程收获：对 AI 辅助软件工程的新认识

这门课最大的收获是：**AI 写代码的能力已经相当强，但 AI 无法替代工程判断**。

AI 能很好地把一个清晰的需求变成可运行的代码，但它不知道这个功能是否真的有价值，不知道这个架构五年后是否还能维护，不知道这个设计决策在产品层面是否正确。这些判断仍然需要人来做。

我现在对"工程师的价值"有了更清晰的认识：不再是"能写多少代码"，而是"能提出多准确的问题、能发现多隐蔽的问题、能做出多好的系统设计"。AI 把代码实现的门槛大幅降低了，但同时也提高了对"想清楚做什么"的要求。

---

## 8. 未来改进：如果重做一次

如果重新来一次，我会在 **idea 阶段多花时间**。这次选择做互动小说平台，功能本身是有趣的，但它的核心价值——"AI 生成故事"——其实对 LLM 的质量依赖很强，而 LLM 的响应质量在不同供应商之间差异很大，导致大量工程时间花在了"如何处理 LLM 乱返回的格式"上，而不是在产品本身的创新上。

如果再做一次，我会选择一个**对 LLM 质量不那么敏感的 idea**——比如代码辅助工具、知识库管理、或者某个垂直领域的信息聚合工具。这类 idea 的核心价值不依赖 LLM 能不能写出优美的叙事，即使模型质量一般，工具本身也能正常运作。

另外，我会更早搭建好本地和部署环境的一致性，避免"本地通过、线上挂掉"的情况反复出现。

---

