# SPEC_PROCESS.md — 规约设计过程文档

> 记录与 Superpowers brainstorming 和 planning 技能协作生成 SPEC 与 PLAN 的全过程。
> 包括关键迭代、冷启动验证、AI 建议与人工决策的记录。

---

## 1. 项目概述

**项目名：** StoryFlow（基于动态叙事规划与分层记忆的持续互动小说生成应用）  
**项目类型：** AI4SE 期末项目 B（非 harness 应用）  
**规约完成时间：** 2026-08-06  
**冷启动验证时间：** 2026-08-07  

---

## 2. Brainstorming 阶段关键节点

### 2.1 核心问题澄清

通过与 AI 的对话，逐步澄清的核心问题：

1. **连续生成 vs. 分段生成** 
   - 初始想法："用户想看持续流式内容"
   - AI 追问："如何避免无限生成导致费用失控？"
   - 最终设定：5 场景后强制选择或安全暂停（§5.2、§9.1 成本护栏）

2. **选择点的判定策略**
   - 初始想法："在情感转折处出现选择"
   - AI 追问："如何定义'转折'，避免机械出现？"
   - 最终设定：模型建议 + 确定性规则（最小/最大间隔 + 冲突关联性检查）（§5.3）

3. **长篇记忆管理**
   - 初始想法："保留全部历史"
   - AI 追问："对大型故事会超出 context 预算，怎么办？"
   - 最终设定：分层记忆 + 滚动摘要 + 优先级裁剪（§5.5、T07）

4. **分支与历史选择**
   - 初始想法："允许回到任意历史选择"
   - AI 追问："分支会指数增长，UI 如何展示？"
   - 最终设定：分支列表 + 简化的选择历史；MVP 不画完整树（§5.6、P2 限制）

---

### 2.2 重要的 AI 建议（已采纳）

| AI 建议 | 采纳状态 | 理由 |
|--------|---------|------|
| 分离"导演"和"作者" LLM 角色 | ✅ 已采纳 | 导演规划情节，作者写正文，职责分离便于质量控制 |
| 使用 Pydantic 验证模型输出 | ✅ 已采纳 | 结构化校验降低模型误输出风险，失败自动重试（§5.1、T05） |
| 幂等键防止重复场景 | ✅ 已采纳 | 网络中断时不会重复计费或产生重复内容（§5.2、T10） |
| 匿名会话隔离而非注册登录 | ✅ 已采纳 | MVP 简化 auth，用签名 Cookie 隔离（§5.7、T17） |
| 导出时仅当前分支，不含兄弟分支 | ✅ 已采纳 | 避免导出混乱，保持简洁（§5.6、§12 AC-08） |

---

### 2.3 人工推翻或修改的 AI 建议

| AI 建议 | 人工决策 | 理由 |
|--------|---------|------|
| 使用 WebSocket 保持长连接流式生成 | ❌ 推翻 | POST + text/event-stream 足够，WebSocket 增加复杂度（§10） |
| 支持多个 LLM 供应商路由 | ❌ 推翻为 P2 | MVP 专注单一供应商（OpenAI / Claude），多供应商后续迭代（§2.2） |
| 细粒度到句子的编辑器 | ❌ 推翻为 P2 | 十天内不可行，仅支持重新生成最近一个场景（§2.2） |
| 使用 LangChain agent 框架 | ❌ 推翻 | 自己实现生成管线便于理解和控制，避免黑盒框架（§10） |

---

## 3. 冷启动验证记录（CS-01）

### 3.1 验证方案

- **时间：** 2026-08-07
- **使用 Agent 类型：** general-purpose（不同于主开发的 Claude Code）
- **Session：** 全新，无历史对话导入
- **提供物：** SPEC.md + PLAN.md + 冷启动指令
- **选定任务：** T02（领域模型与状态机）或 T04（LLM 抽象与 fake LLM）
- **时长：** 约 1～2 小时

### 3.2 验证进行中...

**状态：** ⏳ Agent 正在执行  
**启动时间：** 2026-08-07 08:35 UTC  
**预期完成：** 2026-08-07 10:35 UTC

**待记录项：**
- [ ] Agent 首次暂停位置与问题
- [ ] 对 SPEC 各节的理解差距
- [ ] 产出与预期的具体差距
- [ ] 规约修订建议
- [ ] 验证通过 / 需要进一步调整

---

### 3.3 冷启动结果分析（待完成）

```
[Agent 完成后填入]

A. 暂停点分析
   位置：[T02 / T04 的第几步]
   问题：[Agent 遇到的具体卡点]
   来源：[SPEC 缺陷 / Agent 误读 / 工具缺失]

B. 规约缺陷识别
   问题 1：[具体描述]
   位置：SPEC.md / PLAN.md §X
   
   问题 2：[具体描述]
   位置：...

C. 产出验证
   预期：[T02 / T04 的完成定义]
   实际：[Agent 完成了什么]
   缺口：[什么部分未实现或不符合预期]

D. 规约修订
   修订前：[原文]
   修订后：[新文]
   原因：[冷启动暴露的缺陷]
```

---

## 4. SPEC / PLAN 的已知问题清单

（根据主开发 Claude 的初步检查，在冷启动前梳理）

### 已确认的需补充项

- [ ] **Open Design 选择：** SPEC §10 缺少具体设计系统或 skill 名称
  - 建议：补充"使用 [Open Design 系统名] / 自定义阅读优化"
  - 优先级：**中**（不阻塞实现，但需在 T14 前明确）

- [ ] **凭据存储 CLI：** SPEC §9.2 缺少本地运行的 CLI 命令格式
  - 建议：补充 `storyflow credentials set/status/update/clear` 的详细说明
  - 优先级：**高**（T18 实现时需要确切接口）

- [ ] **导出性能风险：** SPEC §5.7 缺少超大分支的处理说明
  - 建议：补充"超过 50K 字符的导出返回错误"的说明
  - 优先级：**低**（T20 前补充即可）

- [ ] **Fake LLM 流中断：** PLAN T04 缺少模拟流中断的说明
  - 建议：补充"fake LLM 能模拟 connection close 并返回 partial text"
  - 优先级：**中**（T10 会依赖这个能力）

### 冷启动可能暴露的新问题

（待 Agent 完成）

---

## 5. 规约迭代历史

### 迭代 1：初始 Brainstorming 阶段（2026-08-06）

**产出：** SPEC.md + PLAN.md 初版（§1～§17）

**关键决策：**
- 确定项目类型为 B 类应用（不是 agent harness）
- 确定使用 FastAPI + SQLite + fake LLM 技术栈
- 确定十天完成目标和 P0/P1/P2 优先级划分

**参考资源：**
- Superpowers brainstorming 技能指导
- 通用要求与 B 类项目要求
- 实际技术团队的工程实践

### 迭代 2：自检与建议生成（2026-08-07）

**产出：** suggestionFromClaude.md（改进建议清单）

**改进方向：**
- 补充 3 项 SPEC 细节（Open Design、凭据 CLI、导出风险）
- 补充 4 项 PLAN 改进（进度控制、task 拆分、fake LLM 映射、UI 并行）

**优先级排序：**
1. 启动 CS-01 冷启动验证（即刻）
2. 根据冷启动反馈修订 SPEC（Day 1）
3. 补充改进建议中的细项（Day 1 之后）

### 迭代 3：冷启动验证与修订（2026-08-07）

**产出：** CS-01 验证结果 → 规约修订

**工作流：**
1. general-purpose Agent 在全新 session 中独立推进 T02 / T04
2. 记录首次暂停、提问列表、理解差距
3. 人工评判：规约问题 vs. Agent 误读
4. 修改 SPEC.md / PLAN.md 中的缺陷
5. 本节文档更新：修订前后 diff

**通过标准：** Agent 能在 SPEC + PLAN 基础上独立推进至少 30% 的 task，明确表达卡点。

---

## 6. 关键设计决策回溯

### 为何选择分层记忆而非全文上下文？

**背景：** SPEC §5.5 采用分层记忆而非单纯保留全部历史正文。

**决策过程：**
1. 初始想法："简单保留全部历史"
2. AI 指出："超过 30 个场景会爆 context 预算，成本倍增"
3. 比较方案：
   - 方案 A：摘要模型 → 引入额外 API 调用，10 天内难实现
   - 方案 B：规则化摘要 → 不依赖第二个模型，更可控
   - 方案 C：分层记忆（固定 + 动态 + 最近 + 摘要）→ 最灵活
4. 选定方案 C，理由：
   - 在规则化摘要基础上分级存储，避免关键信息丢失
   - 最近 2 个场景保留原文，长期历史用摘要
   - 优先级裁剪确保世界规则、当前弧、活跃伏笔不可被删除
   - MVP 可运行，后续迭代可优化摘要生成

### 为何不用现成 agent 框架（LangChain / AutoGen）？

**背景：** SPEC §10 和 PLAN.md 强调自己实现生成管线。

**决策过程：**
1. 初始考虑："用 LangChain agent 简化开发"
2. 课程要求反思："student 应理解 agentic SE 的核心，不是调包"
3. 权衡：
   - LangChain：快速但黑盒，难以追踪 LLM 调用和故事状态
   - 自己实现：缓慢但透明，能完全控制生成管线每一步
4. 选定自己实现，理由：
   - StoryFlow 不是通用 agent，而是专用管线：导演→作者→记忆更新
   - 需要对状态机、幂等性、中断恢复有精细控制
   - 理解"如何让 LLM 按要求生成结构化内容"是课程核心
   - 框架越薄越好，便于测试和修改

---

## 7. 与技术选型的关联

| SPEC 设计决策 | 对应技术选择 | PLAN 任务覆盖 |
|------------|---------|----------|
| 分离导演/作者 LLM 角色 | 自定义生成管线 | T08（协调器）、T04（fake LLM） |
| 状态机控制流 | Python + Pydantic 域模型 | T02（状态机）、T03（持久化） |
| 分层记忆 + 优先级裁剪 | 上下文构建器 + 字符估算 | T07（上下文预算） |
| 幂等、断线恢复 | 生成键 + 事务 + 启动恢复 | T10（恢复机制） |
| 匿名会话隔离 | 签名 Cookie + 数据库 session_id | T17（会话隔离） |
| Docker 分发 | 不在镜像中存 Key，使用 secret 文件 | T22（Docker 分发） |

---

## 8. 预期收获与反思提示

完成本项目后应能回答的问题：

### 来自 Superpowers 工作流的收获

1. **brainstorming 与 specification：** 
   - AI 追问如何让初模糊想法变清晰？
   - 哪些模棱两可的地方最容易在冷启动中暴露？
   - 规约清晰度与实现效率的关联？

2. **冷启动验证的价值：**
   - 陌生 agent 与主开发 agent 遇到的问题有什么区别？
   - 是否存在"隐性假设"（主开发者知道但写在纸面上）？
   - 单人项目中如何用"冷启动"模拟"同侪评审"？

3. **plan 的可执行性：**
   - Task 颗粒度是否真的控制在 2～5 分钟步骤内？
   - 实际与计划偏离时如何降级？

### 来自代码与工程的反思

1. **状态机的价值：** 
   - 为何在 LLM 应用中明确的状态转换比"按感觉调用 LLM"更重要？

2. **TDD 在 AI 协作下的作用：**
   - 是否能更早发现规约问题？

3. **fake LLM 的可测试性：**
   - 如何验证生成管线逻辑而不依赖网络？

（更多反思内容见最终 REFLECTION.md）

---

## 9. 文档版本历史

| 版本 | 日期 | 关键变化 | 状态 |
|------|------|---------|------|
| 0.1 | 2026-08-07 | 初版创建，框架搭建 | 待填充 |
| 0.2 | 2026-08-07（冷启动后） | 冷启动结果与规约修订 | 待完成 |

---

*文档维护：主开发 Claude（Claude Code） 与冷启动 Agent（general-purpose）*  
*最后更新：2026-08-07*
