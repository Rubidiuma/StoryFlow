# SPEC.md / PLAN.md 改进建议

基于 AI4SE 期末项目通用要求与 B 类应用项目规范的检查结果。

## SPEC.md 需补充的细节

### 1. Open Design 选择不够具体

**问题：** §10 技术选型表的"UI 方法"行写的是：
```
UI 方法 | Open Design 中适合阅读型 Web 的设计原则 | 满足课程对 UI 项目的建议，优先可读性和状态清晰度
```

但没有明确**选择了哪个 Open Design 设计系统或 skill**。课程要求 §3.6 说"在 SPEC 中说明所选设计系统与 skill"。

**建议修改：**

在 §10 表格中改为：
```
UI 方法 | Open Design 系统 [具体选项] / 自定义阅读优化 | 说明选择理由
```

或在 §11 "在线 WebUI" 新增小节 "UI 设计系统"：
```markdown
### UI 设计系统

选择 [Open Design 系统名 / 自定义方案]，原因是 [阅读沉浸感 / 中文排版 / 状态清晰度 / 其他]。
前端框架：HTML/CSS + 轻量 JavaScript（见 §10）。
字号、行高、阅读宽度等默认值参考 [Open Design wiki / 项目实践]。
```

**影响：** 后续 T14 实现 Web UI 时 subagent 会明确参照什么设计系统。

---

### 2. 凭据存储方案：本地原生运行缺少具体接口

**问题：** §9.2"对策"中写了"本地原生运行优先使用操作系统 Keychain/Secret Service，通过隐藏输入命令提供 `set/status/update/clear`"，但没有说明：
- CLI 命令格式是什么
- 查看状态时是否返回明文（说"不回显明文"，但也没说返回什么）
- 失败时的错误消息

**建议修改：** 在 §11 新增"凭据配置"小节，补充本地运行示例：

```markdown
### 本地原生运行：凭据配置

#### CLI 命令

```bash
# 设置（首次运行引导或手动更新）
storyflow credentials set --provider openai   # 或 claude / 其他供应商

# 查看状态（不回显明文，仅显示已配置/未配置）
storyflow credentials status
# 输出：OpenAI API Key: configured (set at 2026-08-07 10:00 UTC)

# 更新（替换当前凭据）
storyflow credentials update --provider openai

# 清除
storyflow credentials clear --provider openai
```

#### 存储位置与安全

- **优先级顺序：**
  1. macOS Keychain / Linux Secret Service / Windows Credential Manager（系统级，最安全）
  2. 项目目录 `.env.local`（仅在本机开发时使用，加入 .gitignore，明文风险）
  3. 环境变量 `STORYFLOW_OPENAI_KEY`（明文进入进程环境，**不推荐用于生产**)
  
- **首次启动：** 如果未检测到凭据，应用启动时会提示"需要配置凭据，请运行 `storyflow credentials set`"。

- **查看状态不回显明文：** 仅显示"configured at [时间]"，不显示 key 本身。
```

**影响：** 实现 T18（凭据管理）时 subagent 知道确切要做什么。

---

### 3. 导出功能在线部署的性能风险

**问题：** §5.7 和 §12（AC-08）中要求"导出只包含当前分支路径"，但没有说明：
- 如果一个故事有 100 个片段，导出会不会超时
- 前端是否应该有加载指示器
- 超大导出是否应该分批

**建议修改：** 在 §9.1（性能）新增：

```markdown
### 导出性能

- 单个分支导出（< 50K 中文字符，约 30 个片段）目标响应时间 < 2 秒。
- 超过 50K 字符的导出：
  - 应用返回 400 Bad Request，提示用户"分支过长，请从历史选择创建子分支后导出"。
  - 或提供流式导出（未来需求）。
```

同时在 §11 "在线 WebUI" 中补充：
```markdown
为避免单次导出消耗过多资源，公共部署限制：
- 单个分支最多导出 50K 字符
- 若用户故事超过此限制，引导其从历史选择创建新分支
```

**影响：** T20（Markdown 导出）实现时明确处理边界情况。

---

## PLAN.md 需要改进的地方

### 1. 冷启动验证 (CS-01) 是强制前置

**现状：** PLAN.md §3 中 CS-01 标记为 `[ ]` 未开始。

**要求（通用要求 §4.5）：** 
> 正式实现前，用**一个与主开发智能体不同**的 agent，在**不向其提供你与主 agent 对话历史**的前提下，仅凭 `SPEC.md` + `PLAN.md` 尝试实现 1–2 个 task（约 1–2 小时）。

**行动：** 启动 CS-01，步骤如下：
- [ ] 创建新 worktree `coldstart/spec-plan-validation`
- [ ] 用不同类型的 Agent（非 Claude Code）启动全新 session
- [ ] 仅提供 `SPEC.md`、`PLAN.md` 和指令
- [ ] 记录 Agent 在哪里暂停、提出了什么问题
- [ ] 对比预期，判断是规约问题还是 Agent 误读
- [ ] 修订 SPEC.md / PLAN.md 中暴露的歧义
- [ ] 在 `SPEC_PROCESS.md` 中记录：
  - 暂停点位置
  - Agent 的问题和解读
  - 产出与预期的差距
  - 修订前后的关键 diff
  - 人工判断和决策

**预期成果：** `SPEC_PROCESS.md` 第一节"冷启动验证"

---

### 2. 某些 Task 颗粒度过大，可考虑拆分

**情况 1：T07（上下文预算与分层记忆）**

预计 90 分钟，但实际包含：
- 字符/token 估算接口（15 分钟）
- 优先级裁剪逻辑（20 分钟）
- 记忆结构序列化（15 分钟）
- 摘要触发和生成（20 分钟）
- 集成测试（20 分钟）

**建议：** 如果进度压力大（Day 4 发现落后），可拆为：
- **T07a：字符估算与优先级裁剪**（60 分钟，P0）
- **T07b：摘要生成与记忆更新队列**（60 分钟，P1）

当前标记 PR-03 依赖 T07；修改后可标记 PR-03 -> T07a、PR-04 -> T07a/T07b。

**情况 2：T21（端到端测试与演示夹具）**

预计 90 分钟，但包含了编写 fake LLM 脚本、E2E 流程测试、更新 README。可拆为：
- **T21a：演示脚本与单流程**（45 分钟）
- **T21b：回归测试与文档**（45 分钟）

**建议：** 在 §5 任务索引中添加注释说明哪些任务在时间压力下可拆分。

---

### 3. 缺少 Fake LLM 与集成测试的映射

**问题：** T04（LLM 抽象与 fake LLM）的失败测试写的是：
```
**失败测试：** fake LLM 能按脚本返回 JSON、逐块流式文本、模拟超时/非法结构，并能断言收到的上下文。
```

但"流中断"这个关键场景只在 T10 中验证，而 fake LLM 如何模拟网络中断没有说清。

**建议修改 T04 的失败测试：**

```markdown
**失败测试：**

- 按脚本返回 JSON、逐块流式文本。
- 模拟超时、非法 JSON 结构。
- 模拟流中断（close connection）并返回 partial text。
- 能记录收到的每次请求上下文，用于后续验证。
```

同时在 T04 的步骤中补充：
```markdown
- [ ] 写流中断模拟与半数据处理测试（后续 T10 会用）。
```

**影响：** T10 实现时能复用 fake LLM 的中断能力，而不是临时写。

---

### 4. 依赖链关键路径较长，建议提前静态 UI

**现状：** T14（创建向导和书架）、T15（流式阅读器）依赖 T05（故事创建）和 T09（流式接口），时间线：
```
T05: Day 3
T09: Day 5
T14: Day 7
T15: Day 7
```

**建议：** 从 Day 3 起就开始静态 UI 框架（不依赖真实 API），用 fake API mock：

在 §5 任务索引后补充：

```markdown
### UI 并行开发策略

T14 的前 50% 工作（页面布局、导航、样式）可从 Day 3 起用 fake HTTP 响应独立进行：
- 创建 `tests/fixtures/api_mock.py` 提供静态故事数据
- 页面路由和静态 HTML 不依赖后端服务
- Day 5（T09 完成）后接入真实生成接口

这样若 Day 5 后端延期，前端骨架仍可提前完成。
```

同时在 §7（十天日历）中补充：

```markdown
| Day 3 下午 | T04、T06 完成后启动 T14 静态壳（HTML/CSS/路由） | —— |
| Day 5 | T09 完成后，T14 接入真实生成 API | —— |
```

**影响：** 若 Day 5 后端卡壳，前端仍可自测；增加并行度。

---

### 5. 十天日历中进度控制措施需要更明确

**现状：** §10 说"若 Day 6 结束时 T10 尚未完成，立即删除人物/伏笔侧栏"。

**建议补充更细的降级方案：**

```markdown
### 进度控制与降级策略

**Checkpoint 1（Day 4 下午）：**
- 目标：PR-02 (T04～T05) 和 PR-03 (T06～T07a) 完成。
- 若 T07b 未开始：标记为 P2，推到 Day 8 之后。

**Checkpoint 2（Day 6 下午）：**
- 目标：PR-04 (T08～T10) 完成，生成管线工作。
- 若 T10 未完成：立即停止后续功能，专注恢复能力。

**Checkpoint 3（Day 8 下午）：**
- 目标：T14～T16 (基础 UI) 完成。
- 若 T16 选择/分支 UI 未完成：
  - 保留后端选择提交和分支创建接口
  - 将 UI 降级为"简单列表和表单"（删除分支树、人物侧栏、动画）

**Checkpoint 4（Day 9 上午）：**
- 目标：T21（E2E）、T22（Docker）、T23（CI）三项必做完成。
- 若其中任何一项受阻：立即停止其他工作，集中突破。

**不可删除项**（任何情况下都要保留）：
- 凭据安全（T18）
- 幂等与恢复（T10）
- 测试 + CI（T21、T23）
- Docker 与 README（T22、T24）
```

---

## 冷启动验证 (CS-01) 计划

**何时启动：** 现在（Day 1）

**目标：** 用独立 Agent 验证 SPEC.md / PLAN.md 的清晰度，暴露规约缺陷。

**步骤：**

1. 创建冷启动 worktree：`coldstart/spec-plan-validation`
2. 用不同类型 Agent 启动全新 session（非 Claude Code）
3. 仅提供给 Agent：
   - SPEC.md
   - PLAN.md
   - 指令："从 PLAN 的 T02 或 T04 中选择 1～2 个任务推进；遇到不确定处立即暂停，不得自行补充产品规则。"
4. Agent 推进 1～2 小时，记录：
   - 在哪个步骤首次暂停
   - 提出的问题列表
   - 对规约的不同解读
5. 人工评判：
   - 问题来自规约缺陷还是 Agent 误读？
   - 产出与预期差距有多大？
   - 需要修改哪些规约部分？
6. 修订 SPEC.md / PLAN.md
7. 记录所有过程到 `SPEC_PROCESS.md`（第一节"冷启动验证"）

**预期产出：**

```
SPEC_PROCESS.md
├── 1. 冷启动验证
│   ├── Agent 类型：[不同于主开发的类型]
│   ├── 首次暂停点：[Task / Step]
│   ├── Agent 提出的问题：[列表]
│   ├── 规约修订：[修改前后 diff]
│   └── 验证结果：[通过 / 需要再改进]
```

**成功标准：** Agent 能在 SPEC + PLAN 基础上独立推进至少 30% 的 Task，并清晰表达遇到的卡点。

---

## 总结

| 优先级 | 改进项 | 预计工作量 |
|--------|--------|----------|
| **即刻** | 启动 CS-01 冷启动验证 | 2～3 小时 |
| **Day 1 之后** | 补充 SPEC.md 的 3 项细节 | 30 分钟 |
| **Day 1 之后** | 补充 PLAN.md 的进度控制措施 | 20 分钟 |
| **可选** | 考虑拆分 T07、T21 | 10 分钟（规划） |

---

*文档生成时间：2026-08-07*  
*基于 Claude Haiku 4.5 自动检查结果*
