# StoryFlow 产品与技术规约

> 基于动态叙事规划与分层记忆的持续互动小说生成应用  
> 项目类型：AI4SE 期末项目 B（非 harness AI 应用）  
> 状态：PR-01（T01～T03）实现完成，待后续功能 PR
> 目标周期：10 天

## 1. 问题陈述

### 1.1 背景

现有 AI 小说产品大多采用“一次生成一段、用户手动要求续写”或“预先生成固定章节”的交互。前者缺少阅读沉浸感，后者又难以让用户在关键剧情节点真正改变故事。直接把全部历史正文反复发送给模型，还会随着篇幅增长带来上下文超限、成本上升、人物失忆和伏笔丢失。

### 1.2 要解决的问题

StoryFlow 要提供一种类似聊天流的持续阅读体验：用户完成初始设定并点击开始后，正文持续流式生成；系统只在具有真实叙事意义的决策点暂停并给出选项；用户选择或输入自定义行动后，故事沿新方向继续。作品可以跨越固定章节长期续写，并保持人物状态、世界规则、伏笔和历史选择的一致性。

### 1.3 目标用户

- 喜欢互动小说、文字冒险和角色扮演故事的普通读者。
- 有基本故事构想，但不擅长完整写作的创作者。
- 希望探索同一故事不同走向的用户。

### 1.4 产品价值

- 阅读过程连续，不需要反复输入“继续”。
- 选择出现于真正影响剧情的节点，而非机械地每段弹出。
- 不同选择会改变人物关系、已知信息、行动路线或后续故事弧。
- 通过分层记忆支持长期续写，而非依赖无限增长的原文上下文。
- 用户可以回到历史选择创建新分支，不破坏原有故事线。

## 2. 项目目标与非目标

### 2.1 MVP 目标

1. 提供题材、结构、世界观、主角、文风和互动频率的创建向导。
2. 基于故事圣经和动态故事弧持续生成中文小说正文。
3. 正文通过流式接口逐步显示；无选择点时前端自动请求下一场景。
4. 在满足叙事条件和频率约束时暂停，提供 3 个有真实差异的选项。
5. 支持用户输入自定义行动并作为正式选择继续故事。
6. 维护人物状态、未解决冲突、伏笔、历史选择和滚动摘要。
7. 支持暂停、继续、刷新恢复、从历史选择创建分支以及 Markdown 导出。
8. 提供可一键运行的测试、Docker 分发、CI 和可访问 WebUI。

### 2.2 非目标

- 不生成图片、音频或视频。
- 不提供多人协作、社区发布、付费订阅或版权交易。
- 不承诺一次生成数十万字，也不预先生成整本小说。
- 不做复杂的可视化分支树，只提供选择历史和分支列表。
- 不支持模型自主联网检索、调用外部工具或修改文件。
- MVP 不支持多模型路由和自动模型选择。
- MVP 不提供细粒度到句子的编辑器；只允许重新生成最近一个未产生后续内容的场景。

## 3. 产品边界：为什么属于 B 类

StoryFlow 是固定编排的 AI 内容生成应用，而不是 agent harness：

- 每次小说会话由用户显式启动，用户可以随时暂停。
- 用户负责决定剧情分支，模型不能替用户完成选择。
- 后端按固定的“规划—写作—提交—记忆更新”管线调用 LLM。
- LLM 不自主调用文件、Shell、浏览器、网络或其他外部工具。
- 后端不会让模型自行决定无限循环；连续生成由确定性状态机和场景上限控制。
- 任何模型调用失败均由应用定义的重试、降级和停止策略处理。

因此产品材料统一使用“AI 互动小说应用”“生成管线”等表述，不称其为自主 Agent。

## 4. 用户故事

以下故事保持彼此独立、可协商、有价值、可估算、规模较小并具有可测试验收条件。

### US-01 创建小说

作为一名互动小说读者，我希望选择题材、叙事结构、主角、世界观、文风和选择频率，以便生成符合个人兴趣的故事。

验收：必填项通过校验后可创建小说；非法或缺失配置得到明确提示；创建成功后生成可查看、可修改的故事圣经。

### US-02 连续阅读

作为读者，我希望点击一次开始后正文能够持续向下生成，以便不必反复输入“继续”。

验收：一个场景流式结束且没有选择点时，客户端自动请求下一场景；用户点击暂停后不再发起新请求。

### US-03 关键选择

作为读者，我希望只在真正影响剧情的节点看到选项，以便我的决定能够改变故事，而不是完成形式化点击。

验收：选择满足最小/最大间隔规则；每次提供 3 个不同选项；选择效果至少改变行动路线、人物关系、信息状态或故事弧之一。

### US-04 自定义行动

作为读者，我希望不受预设选项限制，可以输入自己的行动，以便获得更自由的故事走向。

验收：自定义行动通过长度与内容校验后保存为选择；后续场景上下文包含该行动；非法输入不会改变故事状态。

### US-05 长期续写

作为长期阅读者，我希望故事写得很长后仍能记住角色、世界规则和伏笔，以便持续获得连贯体验。

验收：生成上下文使用分层记忆且不超过预算；人物状态、活跃伏笔和近期选择可被后续场景引用；旧正文仍可在作品中查看。

### US-06 回到历史选择

作为探索型读者，我希望回到一个历史选择并选择另一条路，同时保留原故事线，以便探索不同结局。

验收：系统创建新分支并恢复该选择前的记忆快照；原分支内容不被删除或覆盖。

### US-07 刷新恢复

作为读者，我希望刷新或重新打开页面后继续上次阅读，以免生成内容和选择丢失。

验收：刷新后恢复当前故事、分支、片段、等待选择/暂停状态；不会重复提交同一场景。

### US-08 导出作品

作为创作者，我希望把当前分支导出为 Markdown，以便离线阅读和继续编辑。

验收：导出内容按路径顺序包含标题、故事设定摘要、正文和用户选择，不混入其他分支正文。

## 5. 功能规约

### 5.1 小说创建与故事圣经

### 输入

- 题材：奇幻、科幻、悬疑、情感、开放自定义。
- 结构：三幕式、英雄之旅、悬疑调查、情感成长、开放式连载。
- 世界背景、主角描述、期望文风。
- 选择频率：少、中、多。
- 可选：重要配角、希望出现的元素、禁止出现的元素、结局倾向。

### 行为

1. 客户端完成字段校验。
2. 后端调用 LLM 生成结构化故事圣经，包括世界规则、主要人物、初始冲突、文风约束和第一个故事弧。
3. 使用 Pydantic 校验结构；结构不合法时最多重试一次。
4. 用户确认故事圣经后才可开始正文；用户可在开始前修改或重新生成。

### 输出

- 故事记录、故事圣经、人物初始状态和首个故事弧。

### 边界与错误

- 用户输入按字段限制长度，总输入不超过 6,000 字符。
- 模型拒绝、超时或结构持续非法时不创建半成品故事，返回可重试错误。
- 题材和自由文本需通过基础内容安全检查。

### 5.2 连续场景生成

### 输入

- 当前故事圣经、故事弧、人物状态、活跃伏笔、历史选择摘要、滚动摘要和最近 2 个场景原文。

### 行为

1. “剧情导演”调用生成结构化 `ScenePlan`。
2. 服务端校验计划、选择点和上下文预算。
3. “小说作者”根据计划生成正文，通过流式 HTTP 响应发送给客户端。
4. 流结束后以数据库事务提交场景、摘要和状态变化。
5. 若本场景没有选择点且用户未暂停，客户端自动请求下一场景。
6. 若有选择点，故事进入 `WAITING_CHOICE`，停止自动请求。

### 输出

- 500～1,000 个中文字符的场景正文、场景摘要和可能的选择点。

### 边界与错误

- 单个生成请求只提交一个场景；“持续”由客户端串联请求实现，不维持永久连接。
- 每次流式请求带幂等键，重试不得重复创建场景。
- 请求中断时，未提交场景标为失败，不作为正式正文；用户可重试。
- 一个自动生成批次最多连续生成 5 个场景，随后必须出现选择或进入安全暂停，防止无人值守消耗。
- 同一故事同一分支同一时间只允许一个生成任务。

### 5.3 选择点判定

### 输入

- `ScenePlan` 中的选择建议、距离上次选择的场景数、当前冲突、故事弧阶段和用户频率配置。

### 行为

选择点采用“模型建议 + 确定性规则”决定：

- 少：通常每 4～5 个场景一次。
- 中：通常每 2～3 个场景一次。
- 多：通常每 1～2 个场景一次。
- 未达到最小间隔时，拒绝模型的选择建议。
- 达到最大间隔且当前场景结束完整时，要求计划器产生选择点。
- 选择必须关联明确冲突，并改变行动路线、人物关系、信息状态或故事弧。
- 每个选择点生成 3 个语义明显不同的选项，禁止“正确/错误/无意义等待”式假选择。

### 输出

- `ChoicePoint` 及 3 个 `ChoiceOption`；选项保存结构化隐藏效果，但 UI 只显示自然语言。

### 错误

- 选项重复、结构非法或无有效影响时最多重试一次；仍失败则安全暂停并允许用户直接输入行动。

### 5.4 用户选择与自定义行动

### 输入

- 预设 `option_id`，或者 1～300 字符的自定义行动。

### 行为

- 只有 `WAITING_CHOICE` 状态可接受选择。
- 选择使用一次性版本号进行并发校验，重复点击只提交一次。
- 预设选择应用隐藏状态影响；自定义行动由一次结构化解析调用转换为规范化影响。
- 选择成功后创建新的记忆快照，状态回到 `IDLE`，客户端可继续生成。

### 错误

- 过期选项、重复提交、空输入或超长输入返回明确错误，不改变状态。
- 自定义行动解析失败时保留用户原文，允许重新提交或选择预设项。

### 5.5 分层记忆与动态故事弧

### 记忆层

1. 固定记忆：世界规则、文风、禁区和主角核心设定。
2. 当前故事弧：目标、主要冲突、阶段和退出条件。
3. 人物状态：位置、关系、秘密、已知信息、动机和存活状态。
4. 伏笔：已埋下、活跃、已回收、放弃。
5. 历史选择：选择文本、隐藏影响和结果摘要。
6. 滚动摘要：每 5 个场景压缩一次；每个故事弧结束后生成高层摘要。
7. 最近正文：默认最近 2 个场景全文。

### 上下文预算

- 上下文构建器按优先级选择内容，并记录估算 token 数。
- 固定设定、当前弧、相关人物和最近选择不可被裁掉。
- 较早正文只使用摘要；已回收伏笔移出活跃上下文但保留数据库记录。
- 达到预算时优先裁剪无关人物和旧场景摘要，不截断结构化 JSON。

### 动态故事弧

- 当前弧达到退出条件后，系统基于未解决冲突、人物目标和历史选择生成下一故事弧。
- 新故事弧不得否定已确认世界规则或已发生事件。
- 产品不设置固定总章节数，用户可持续生成新故事弧。

### 5.6 分支、回退与重新生成

- 正文片段创建后不可原地修改，分支路径由父片段关系组成。
- 从历史选择重新选择时创建新 `Branch`，复用分叉前片段并恢复选择前 `MemorySnapshot`。
- 原分支保持可读。
- 只允许重新生成当前分支最后一个场景，且该场景之后不能已有选择或后续片段；重新生成作为新版本保留旧记录。
- MVP 仅提供分支列表和选择历史，不绘制完整分支图。

### 5.7 书架、恢复与导出

- 首页显示当前匿名会话下的小说、更新时间、当前状态和最近阅读位置。
- 在线演示使用签名的 HttpOnly 匿名会话 Cookie 隔离不同访问者的数据。
- 本地模式使用同一数据库持久化数据。
- Markdown 导出只遍历当前分支路径，按时间顺序写入设定摘要、正文和已选选项。

### 5.8 暂停与恢复

- 用户可在任何时刻设置 `pause_requested`。
- 正在流式生成时，暂停默认在当前场景结束后生效，避免保存半段正文。
- 用户主动取消当前请求时，未完成内容不提交为正式场景。
- `PAUSED` 状态下只有显式继续操作才能恢复自动生成。

## 6. 状态机

故事运行状态包括：

- `DRAFT`：故事圣经尚未确认。
- `IDLE`：可开始或继续生成。
- `PLANNING`：正在生成场景计划。
- `STREAMING`：正在流式生成正文。
- `COMMITTING`：正在提交场景和记忆变化。
- `WAITING_CHOICE`：等待用户选择，禁止继续生成。
- `PAUSED`：用户暂停或达到安全上限。
- `ERROR`：不可自动恢复的错误，等待重试。

关键约束：

- 任意时刻一个故事分支最多存在一个活动生成任务。
- `WAITING_CHOICE` 不可直接进入 `PLANNING`。
- 只有场景成功提交后，客户端才能自动请求下一场景。
- 所有状态转换写入事件日志，便于排查重复生成和中断恢复问题。

## 7. 系统架构

```text
浏览器 WebUI
  ├── 创建向导
  ├── 流式阅读器
  ├── 选择与自定义行动
  ├── 人物/伏笔侧栏
  └── 书架、分支和导出
          │ HTTPS / streaming response
FastAPI 应用
  ├── Story API
  ├── Generation Orchestrator
  ├── Choice Policy
  ├── Context Builder
  ├── Branch Service
  ├── Credential Provider
  └── Observability / Redaction
          │
  ┌───────┴────────┐
SQLite          LLM Provider API
```

### 7.1 生成管线

1. `ContextBuilder` 组装受预算约束的上下文。
2. `DirectorService` 请求结构化 `ScenePlan`。
3. `ChoicePolicy` 确定是否接受/强制选择点。
4. `WriterService` 根据最终计划流式生成正文。
5. `StoryService` 以事务提交片段、计划、选择点和事件。
6. `MemoryService` 更新人物、伏笔、摘要和故事弧；失败时进入可重试队列，但不丢失已提交正文。

### 7.2 接口草案

- `POST /api/stories`：创建故事草稿。
- `POST /api/stories/{id}/bible`：生成/重生成故事圣经。
- `POST /api/stories/{id}/confirm`：确认故事圣经。
- `POST /api/stories/{id}/generate`：规划并流式生成一个场景。
- `POST /api/stories/{id}/pause`：请求暂停。
- `POST /api/choices/{id}/select`：提交预设或自定义选择。
- `POST /api/choices/{id}/branch`：从历史选择创建新分支。
- `GET /api/stories/{id}`：恢复故事及当前状态。
- `GET /api/stories/{id}/export.md`：导出当前分支。

## 8. 数据模型

### Story

- `id`, `session_id`, `title`, `genre`, `structure_template`, `style`
- `choice_frequency`, `status`, `current_branch_id`
- `pause_requested`, `created_at`, `updated_at`, `version`

### StoryBible

- `story_id`, `world_rules`, `tone_rules`, `required_elements`
- `forbidden_elements`, `protagonist_core`, `version`

### CharacterState

- `id`, `story_id`, `branch_id`, `name`, `role`, `location`
- `motivation`, `known_facts`, `secrets`, `relationships`, `alive`, `version`

### StoryArc

- `id`, `story_id`, `branch_id`, `goal`, `conflict`, `stage`
- `exit_conditions`, `status`, `summary`

### StorySegment

- `id`, `story_id`, `branch_id`, `parent_segment_id`, `sequence`
- `content`, `summary`, `scene_plan`, `generation_key`, `status`, `created_at`

### ChoicePoint / ChoiceOption

- `ChoicePoint`: `id`, `segment_id`, `type`, `reason`, `status`, `selected_option_id`
- `ChoiceOption`: `id`, `choice_point_id`, `text`, `effects`, `position`

### Branch

- `id`, `story_id`, `parent_branch_id`, `fork_choice_id`
- `fork_segment_id`, `name`, `head_segment_id`, `created_at`

### MemorySnapshot

- `id`, `story_id`, `branch_id`, `segment_id`, `characters`
- `active_threads`, `foreshadowing`, `rolling_summary`, `context_version`

### GenerationEvent

- `id`, `story_id`, `branch_id`, `event_type`, `request_id`
- `duration_ms`, `model`, `input_token_estimate`, `output_size`
- `error_code`, `created_at`

约束：正文、分支和记忆写入使用事务；`generation_key` 唯一；所有外键启用；删除故事采用显式级联服务，不由模型触发。

## 9. 非功能需求

### 9.1 性能

- 点击开始后，正常网络下 5 秒内出现生成状态或首批文本；若超过则显示阶段和可取消操作。
- 流式过程中至少每 2 秒收到文本或心跳事件。
- 书架和故事恢复接口在 1,000 个片段以内目标响应时间小于 500 ms（不含模型调用）。
- 上下文构建时间目标小于 300 ms。
- 单场景输出限制 500～1,000 个中文字符，允许配置但设硬上限。

### 9.2 安全与凭据威胁模型

### 资产与威胁

- LLM API Key：可能通过源码、Git、日志、前端响应、环境或错误栈泄露。
- 用户小说内容：在线演示中可能被其他匿名访问者越权读取。
- 自由文本：可能产生提示注入、违法内容或使模型偏离系统格式。
- 生成接口：可能被自动化滥用造成费用失控。

### 对策

- Key 仅在服务端读取，绝不发送浏览器、写入数据库、日志或错误响应。
- 本地原生运行优先使用操作系统 Keychain/Secret Service，通过隐藏输入命令提供 `set/status/update/clear`；查看状态不回显明文。
- Docker 使用只读挂载的 secret 文件；云部署使用平台 Secret。`.env` 仅作为开发降级方案，加入 `.gitignore`，README 明确其明文和进程环境风险。
- 日志统一经过脱敏过滤器，过滤常见 Key 格式、Authorization Header、Cookie 和用户完整正文。
- 匿名会话使用不可预测、签名、HttpOnly、SameSite Cookie；所有故事查询必须带 `session_id` 条件。
- 对生成接口设置每会话并发限制、速率限制、单次输出上限和自动场景上限。
- 用户输入与模型数据分字段传入；模型输出必须通过结构校验，不把模型输出作为代码或模板执行。
- 不记录完整 Prompt；仅记录模板版本、长度、耗时和错误码。
- 提交前执行 secret scan，仓库和 Git 历史不得出现真实凭据。

### 剩余风险

- 拥有宿主机或容器高权限的进程仍可能读取运行时密钥。
- LLM 供应商会处理发送的故事上下文，应在隐私说明中告知用户不要输入敏感个人信息。
- 纯自动内容安全检查可能误判，MVP 不承诺覆盖所有不当内容。

### 9.3 可用性

- 桌面和手机 Web 均可完成创建、阅读、选择、暂停和继续主流程。
- 生成、规划、等待选择、暂停和失败状态必须有可见反馈。
- 选项支持键盘操作；正文具有适合长时间阅读的字号、行高和宽度。
- 断线或模型错误不能清空已提交正文。
- 用户可在故事创建时查看预计生成成本/消耗提示。

### 9.4 可观测性

- 使用结构化日志记录请求 ID、故事 ID 哈希、阶段、耗时、重试、输出长度和错误码。
- 不记录 API Key、Cookie、完整 Prompt、完整正文和自定义行动原文。
- 记录每次状态转换和幂等命中。
- 提供健康检查接口，区分应用、数据库和 LLM 配置状态；健康接口不得暴露 Key。

### 9.5 可靠性

- 场景和选择提交具有幂等性。
- 模型结构化输出失败最多重试一次；网络瞬时失败按退避策略最多重试两次。
- 应用重启后，遗留的 `PLANNING/STREAMING/COMMITTING` 状态转换为可恢复 `ERROR`，不得自动重复计费生成。
- 数据库事务失败不产生可见半场景。

## 10. 技术选型与理由

| 领域 | 选型 | 理由 |
| --- | --- | --- |
| 语言 | Python 3.12 | 开发周期短，AI/数据校验与测试生态成熟 |
| Web 后端 | FastAPI | 支持异步、流式响应、类型化接口和 OpenAPI |
| 前端 | HTML/CSS + 轻量 JavaScript | 十天内控制工程复杂度，保留完整交互能力 |
| UI 方法 | Open Design 中适合阅读型 Web 的设计原则 | 满足课程对 UI 项目的建议，优先可读性和状态清晰度 |
| 数据库 | SQLite | 单机与 Docker MVP 部署简单，支持事务和关系约束 |
| 数据校验 | Pydantic | 可靠校验导演、选项和记忆等结构化模型输出 |
| 流式协议 | `fetch` 读取 `text/event-stream` | 支持 POST 请求体与逐段解析，无需 WebSocket 复杂度 |
| LLM 接入 | 自定义薄适配层 + 单一供应商 API | 便于 fake LLM 测试，避免引入现成 agent 框架 |
| 测试 | pytest + FastAPI TestClient/fake LLM | 可测试状态机、流式事件、分支和上下文逻辑 |
| 分发 | Docker | 易于在全新机器上运行并满足课程分发要求 |
| CI | GitLab CI | 与最终提交平台和 `unit-test` job 要求一致 |

MVP 不使用 LangChain、AutoGen、CrewAI 等 agent 框架。LLM 适配层只负责一次请求、流式返回和结构化解析。

## 11. 分发与部署设计

### Docker 分发

- 提供 `Dockerfile` 和明确的 `docker build`、`docker run` 命令。
- 数据目录 `/data` 通过 volume 持久化。
- Key 通过只读 secret 文件挂载到 `/run/secrets/llm_api_key`，不打入镜像。
- 镜像使用非 root 用户运行，限制写权限到 `/data`。
- README 说明支持的平台、架构、端口、数据目录、Key 配置及已知限制。

### 在线 WebUI

- 部署到支持容器和 Secret 的平台。
- 平台 Secret 保存服务端 Key。
- 提供截止前可访问 URL、健康检查和部署说明。
- 公共演示开启匿名会话隔离、频率限制和单次生成上限，避免费用失控。

## 12. 验收标准

### AC-01 创建与确认

- 用户可以完成创建向导，生成并确认故事圣经。
- 故事圣经未确认时生成接口返回冲突状态，不生成正文。

### AC-02 持续流式生成

- fake LLM 下，客户端能按顺序展示流式正文事件。
- 一个无选择场景提交后自动生成下一场景。
- 暂停后不再发起下一场景请求。

### AC-03 选择点

- 中等频率下不会在连续两个场景重复出现选择。
- 达到配置的最大间隔后产生选择或安全暂停。
- 每个选择点存在 3 个不重复选项和非空结构化效果。
- `WAITING_CHOICE` 状态调用生成接口会被拒绝。

### AC-04 选择和自定义行动

- 用户只能成功提交一次选择。
- 选择影响进入下一场景上下文。
- 合法自定义行动可以继续故事，非法输入不会修改状态。

### AC-05 长期记忆

- 使用至少 30 个 fake 场景进行测试时，上下文仍不超过配置预算。
- 固定世界规则、活跃人物状态和未解决伏笔仍包含在上下文中。
- 早期原文保存在数据库，但只通过摘要进入模型上下文。

### AC-06 分支

- 从历史选择创建新分支后，分叉前内容共享，分叉后内容独立。
- 新分支使用选择前的记忆快照。
- 原分支内容和当前头节点保持不变。

### AC-07 恢复和幂等

- 页面刷新能恢复当前故事状态。
- 相同 `generation_key` 重试不会新增重复场景。
- 中断请求不会生成可见半场景。

### AC-08 导出

- Markdown 只包含当前分支路径，不包含兄弟分支正文。
- 导出顺序、标题、正文和用户选择正确。

### AC-09 安全

- 自动化测试验证跨会话读取返回 404 或 403。
- API Key 不出现在前端构建、数据库、日志和错误响应中。
- secret scan 在 CI 中通过。

### AC-10 工程交付

- `make test` 或等价命令可一键运行核心测试。
- `.gitlab-ci.yml` 存在名为 `unit-test` 的 job，最后一次 CI 通过。
- Docker 可按 README 从零构建和运行。
- 提供可访问 WebUI URL 和课程要求的全部过程文档。

## 13. 测试策略

### 单元测试

- 故事和输入配置校验。
- 状态机合法/非法转换。
- 选择最小/最大间隔策略。
- 上下文预算、裁剪优先级和摘要选择。
- 分支路径遍历与记忆快照恢复。
- Markdown 导出。
- 日志脱敏和凭据来源优先级。

### 集成测试

- 使用 fake LLM 完成“创建—生成—自动继续—等待选择—提交选择—继续”的完整流程。
- 流式事件顺序、断线、重试和幂等。
- SQLite 事务失败与应用重启恢复。
- 匿名会话数据隔离。

### UI 关键路径

- 创建并确认故事。
- 看到流式正文。
- 暂停和继续。
- 选择预设选项和输入自定义行动。
- 回到旧选择创建分支。
- 刷新恢复并导出。

E2E 只覆盖上述关键路径，不用大量脆弱的页面细节测试替代单元和集成测试。

## 14. 规模控制与十天完成边界

必须优先打通的垂直闭环：

> 创建故事 → 确认故事圣经 → 流式生成 → 自动继续 → 关键选择 → 用户决定 → 新方向继续 → 刷新恢复

P0：创建、流式生成、自动继续、选择状态机、持久化、fake LLM 测试。  
P1：分层记忆、动态故事弧、分支回退、Markdown 导出。  
P2：人物/伏笔侧栏、移动端优化、生成成本展示。

若进度不足，优先删除 P2 视觉增强，不得删除状态机、幂等、凭据安全、测试、CI、Docker 和过程文档。

## 15. 风险与缓解

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| LLM 文笔不稳定 | 小说可读性下降 | 分离导演与作者；固定文风合同；场景目标明确；提供最近场景原文 |
| 选择过频或无意义 | 互动体验机械 | 程序控制间隔；校验选择影响；支持自定义行动 |
| 长篇设定遗忘 | 人物和剧情矛盾 | 分层记忆、状态结构化、滚动摘要、活跃伏笔 |
| 流式中断造成重复内容 | 数据不一致和重复计费 | 一场景一请求、幂等键、提交事务、恢复状态 |
| 自动请求导致费用失控 | 产生不可控成本 | 每批最多 5 场景、选择/安全暂停、速率和并发限制 |
| 分支复杂度迅速增加 | 数据和 UI 难维护 | 不复制全文；父节点路径；MVP 不画完整树 |
| 十天内前后端范围过大 | 无法形成闭环 | 使用轻量前端；优先 P0；只支持一个模型与中文生成 |
| 公共部署被滥用 | Key 费用和数据风险 | 匿名隔离、频率限制、Secret、输出上限、可关闭生成 |

## 16. 未决问题

以下问题不阻塞 `writing-plans`，可在计划第一批任务中通过配置默认值解决：

1. 项目最终中文名采用“StoryFlow”“流岔”还是其他名称；代码仓库暂用 `storyflow`。
2. 首个真实 LLM 供应商及具体模型；架构以 `LLMClient` 薄接口隔离选择。
3. 在线部署平台；默认优先支持容器和 Secret 的平台。
4. 内容安全策略的严格程度；MVP 先实现基础输入限制、供应商安全响应处理和明确错误提示。
5. 场景默认长度和不同选择频率的具体数值；本文给出的范围作为初始默认值，通过测试与人工体验校准。

## 17. 完成定义

只有同时满足以下条件，项目才可宣称完成：

- P0 和 P1 验收标准通过，关键 UI 流程可运行。
- 不连接真实 LLM 时，fake LLM 测试可确定性覆盖完整主流程。
- 真实 LLM 下可演示至少一次选择前后明显不同的连续故事走向。
- 测试、CI、Docker、凭据配置、WebUI 地址和 README 齐全。
- `SPEC.md`、`PLAN.md`、`SPEC_PROCESS.md`、`AGENT_LOG.md` 和本人撰写的 `REFLECTION.md` 齐全。
- 仓库无真实凭据，最后一次 CI/CD 为通过状态。
